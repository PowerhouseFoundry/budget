export default function Finance(){
  return (
    <div className="container">
      <h2 style={{marginTop:0}}>Finance</h2>
      <section className="card" style={{padding:16}}>
        <p>Credit & Loan systems will appear here (next build).</p>
      </section>
    </div>
  )
}
